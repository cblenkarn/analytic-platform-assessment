// ── Rubric Admin render composer ──────────────────────────────────────────
import { renderMatrix, renderRetired } from './matrix.view.js';

export function renderAdminAll() { renderMatrix(); renderRetired(); }
