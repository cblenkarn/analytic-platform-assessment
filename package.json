{
  "name": "merkle-platform-assessment",
  "version": "2.0.0",
  "private": true,
  "description": "Merkle Analytic Platform Assessment — static front-end with Supabase persistence (normalized per-row tables; vanilla ES modules, no build step).",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve .",
    "check": "for f in $(find src scripts -name '*.js'); do node --check \"$f\" || exit 1; done && echo 'syntax OK'",
    "migrate": "node scripts/migrate-to-tables.mjs"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2"
  }
}
