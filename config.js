// Local/dev config. Replace with real values (or provide at deploy time).
// If left blank, pages run offline against the in-code seed (no save/load).
window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
